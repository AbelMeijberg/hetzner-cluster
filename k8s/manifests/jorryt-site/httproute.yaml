apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: jorryt-site
  namespace: jorryt-site
spec:
  parentRefs:
    - name: main-gateway
      namespace: gateway
      sectionName: antwerpen-groundctl-dev
  rules:
    - backendRefs:
        - name: jorryt-site
          port: 80
