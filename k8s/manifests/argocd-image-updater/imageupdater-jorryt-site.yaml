apiVersion: argocd-image-updater.argoproj.io/v1alpha1
kind: ImageUpdater
metadata:
  name: jorryt-site
  namespace: argocd
spec:
  namespace: argocd
  applicationRefs:
    - namePattern: jorryt-site
      images:
        - alias: site
          imageName: ghcr.io/abelmeijberg/jorryt-site
          commonUpdateSettings:
            updateStrategy: newest-build
            allowTags: "regexp:^sha-"
            pullSecret: pullsecret:argocd/ghcr-pull-secret
